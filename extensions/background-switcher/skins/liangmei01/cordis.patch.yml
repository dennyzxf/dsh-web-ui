# liangmei01 skin bundle patch: inserts its dsh.client row into the web plugin roster.
# Mutual exclusion between skins is managed by apply-skin.mjs (home-layer disabled rows).
- insert:
    - id: ui-skin-liangmei01
      name: '@linxin666/dsh-client-ui-skin-liangmei01'
